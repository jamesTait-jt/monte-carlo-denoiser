def generateDarts(num_darts, image_width, image_height, patch_width, patch_height):
    x_lower = patch_height / 2
    x_upper = image_height - patch_height / 2
    
    y_lower = patch_width / 2
    y_upper = image_width - patch_width / 2

    darts = []
    for dart in range(num_darts):

        #generate a random number inside the bounds

    return darts

    
